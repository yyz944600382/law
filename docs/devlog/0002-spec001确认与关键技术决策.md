# 0002 · spec-001 确认与关键技术决策

- **日期**:2026-06-25
- **类型**:需求确认 / 决策

## 背景
用户提交 spec-001(最小 4-agent demo)。Claude 侦察本机环境,就 4 个方向性问题提问,用户已确认。

## 环境侦察结论
- Python 3.13.11 / uv 0.11.11 / Node v23.11.0 / Docker 27.3.1 均就绪。
- `DEEPSEEK_API_KEY` 已设置;pnpm、nginx 未装(Docker 模式不需要)。
- DeepSeek `BASE_URL` 未设,用默认 `https://api.deepseek.com/v1`。

## 决策(详见 spec-001「澄清记录」)
1. **在 deerflow 上开发**(law = deer-flow 定制版)。
2. **Docker** 运行。
3. 法律检索 demo 阶段用 **web-search 凑合**(临时,有幻觉风险)。
4. **Web UI(:2026)**。

## 影响 / 后续
- 下一步实现路线:clone deer-flow → 配 DeepSeek → 起 Docker → 配 4 个 agent(法官 lead / 律师 sub / 法律 skill)。
- 法律检索幻觉风险已记录;正式版需用可控、可溯源法条库替换(届时找用户要数据)。