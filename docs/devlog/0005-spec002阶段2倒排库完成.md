# 0005 · spec-002 阶段2:倒排库完成(minisearch + 中文 bigram)

- **日期**:2026-06-25
- **类型**:进展(spec-002)

## 完成
- 倒排库 `src/law-data/inverted-index.ts`:minisearch(BM25),中文「单字 + bigram」自定义分词。
- 验证(真实数据):
  - 搜「盗窃公私财物」→ **第二百六十四条**(score=977,盗窃罪)
  - 搜「故意杀人」→ **第二百三十二条**(故意杀人罪)
  - 索引覆盖 452 条,结果按 score 降序。
- 测试:4 项通过。

## 经验
- minisearch 默认分词对**中文失效**(连续中文被当成一个 token);用自定义 `tokenize` 做 bigram 解决,「盗窃」能命中「盗窃罪/盗窃金融机构」。
- `combineWith: AND` + `fuzzy: 0.1` 对 bigram 召回既精确又容错。

## 后续
- ⏳ 向量库(embedding 待用户选型,见 plan 0002)。
- ⏳ 检索接口(倒排 + 向量融合,替换联网 retrieval)。
